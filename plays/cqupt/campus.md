---
layout: cols
direction: column
section: Campus
---

# Campus

School logo / badge from built-in presets. `preset` picks the school, `variant` toggles logo / badge; pass `logo` / `badge` URLs to override with your own assets.

::1::

### Variants

<div class="flex h-full items-center justify-center gap-16">
  <Campus />
  <Campus variant="badge" />
</div>

::2::

### Sizes

`size` follows the addon presets — `small` / `medium` (default) / `large`.

<div class="flex h-full items-center justify-center gap-14">
  <Campus size="small" />
  <Campus />
  <Campus size="large" />
</div>
